When the cover option is selected from the sidebar the app should auto switch to the cover view in the priview area.
Use real data for the landing page stats instead of daummy data also add a loading state while the data is being fetched.
Same logic from task 2 should apply to the testimonials section, if there are no testimonials then hide the section and if there are testimonials show them in a carousel. Also add a way to get testimonial
